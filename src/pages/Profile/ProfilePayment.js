import React from 'react';

const ProfilePayment = () => {
    return (
        <React.Fragment>
            <div>ProfilePayment</div>
        </React.Fragment>
    );
};

export default ProfilePayment;
