import React from 'react';

const ProfileSettings = () => {
    return (
        <React.Fragment>
            <div>ProfileSettings</div>
        </React.Fragment>
    );
};

export default ProfileSettings;
